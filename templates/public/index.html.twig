{% extends 'base.html.twig' %}

{% block title %}Hello PublicController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    <h1 class="display-3 mt-3 text-center">Hotel La Casserai</h1>
<div class="row">
<div class="col-md-2"></div>
            <div class="col-md-8">
                {% for kamer in kamers %}
        <div class="card mb-5 text-center">
            <div class="card-header text-center">{{ kamer.omschrijving }}</div>
            <div class="card-body">
                <img src="{{ asset('uploads/' ~ kamer.imagefile)}}" alt="Girl in a jacket" width="700" height="300">
              <h2 class="" style="margin-top: -1.5em;">€{{ kamer.price }}/night</h2>
                <p>Bedden: {{ kamer.bed }}</p>
                <p>{{ kamer.name }} kamer voor {{ kamer.bed }} personen</p>
                <a href="" class="btn btn-outline-info btn-block">Boeken</a>
            </div>
        </div>
        {%  endfor %}
    </div>
    <div class="col-md-2"></div>
</div>
{% endblock %}
